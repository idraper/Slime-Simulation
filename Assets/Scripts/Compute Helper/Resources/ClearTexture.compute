#pragma kernel ClearTexture

RWTexture3D<float4> Source;
uint width;
uint height;

[numthreads(8,8,1)]
void ClearTexture (uint3 id : SV_DispatchThreadID)
{
	if (id.x < 0 || id.x >= width || id.y < 0 || id.y >= height) {
		return;
	}

	Source[int3(id.x,id.y,0)] = 0;
}
